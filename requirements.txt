# Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.7.4
asyncpg==0.29.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# AI/ML (Required for activated systems)
openai>=1.58.1,<2.0.0  # Compatible with langchain-openai 0.3.x
anthropic==0.7.0

# LangChain for AUREA NLU (use latest stable 0.3.x versions)
langchain-openai>=0.3.32,<0.4.0  # Latest stable 0.3.x
langchain-core>=0.3.31,<0.4.0  # Compatible with langchain-openai 0.3.x

# Scheduling (Required for agent scheduler)
apscheduler==3.10.4

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
httpx==0.25.2
aiofiles==23.2.1
aiohttp==3.9.1
requests==2.31.0
google-generativeai==0.8.2
networkx==3.2.1  # Required for AI Knowledge Graph (AUREA dependency)

# Browser Automation (Required for UI Tester Agent)
playwright==1.41.1

# Testing
pytest==8.0.0

# Monitoring (Optional)
# prometheus-client==0.19.0
# sentry-sdk==1.39.1

# Additional dependencies for compatibility
# NOTE: 1.24.x does not publish wheels for Python 3.12; bump for compatibility.
numpy==1.26.4

# Embedded Memory System (RAG) - REMOVED heavy ML deps
# sentence-transformers==2.2.2  # TOO HEAVY - 2GB+ with PyTorch
# Using simple embeddings instead

# ML for AI Training Pipeline (lightweight)
scikit-learn>=1.3.0
