# Core Dependencies
fastapi==0.128.0
starlette==0.49.3
uvicorn[standard]==0.27.1
pydantic>=2.7.4
email-validator>=2.0.0  # Required for pydantic EmailStr
asyncpg==0.29.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
sqlparse==0.5.5

# Auth
PyJWT==2.8.0

# AI/ML (Required for activated systems)
openai>=1.58.1,<2.0.0  # Compatible with langchain-openai 0.3.x
anthropic>=0.39.0,<1.0.0

# LangChain for AUREA NLU (0.3.x ecosystem)
langchain>=0.3.27
langchain-core>=0.3.31
langchain-community>=0.3.29
langchain-openai>=0.3.32
langchain-anthropic>=0.3.0
langgraph>=0.6.0

# Scheduling (Required for agent scheduler)
apscheduler==3.10.4

# System Monitoring
psutil>=5.9.0

# Logging (Required for affiliate pipeline and other modules)
loguru>=0.7.0

# Rate Limiting
slowapi>=0.1.9

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.22
httpx>=0.28.1
aiofiles==23.2.1
aiohttp==3.13.3
requests>=2.32.5
google-genai>=1.60.0
google-generativeai>=0.8.0
protobuf>=5.26.1,<6.0.0  # grpcio-status + google-ai-generativelanguage require <6
networkx==3.2.1  # Required for AI Knowledge Graph (AUREA dependency)
ortools>=9.8.0  # Neuro-symbolic logistics solver
dspy-ai>=2.4.0

# Browser Automation (Required for UI Tester Agent)
playwright==1.41.1

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5

# Monitoring (Optional)
# prometheus-client==0.19.0
# sentry-sdk==1.39.1

# Additional dependencies for compatibility
# NOTE: 1.24.x does not publish wheels for Python 3.12; bump for compatibility.
numpy>=1.26.4

# Embedded Memory System (RAG) - REMOVED heavy ML deps
# sentence-transformers==2.2.2  # TOO HEAVY - 2GB+ with PyTorch, causes disk space issues
# Using Gemini gemini-embedding-001 as fallback instead of local model

# ML for AI Training Pipeline (lightweight)
scikit-learn>=1.3.0
scipy>=1.11.0
bleach>=6.0.0

# Email Sending (for ai_email_queue processing)
sendgrid>=6.10.0

# Payment Processing (for affiliate pipeline and webhooks)
stripe>=7.0.0

# Supabase client
supabase>=2.0.0

# Google Keep sync (Gemini Live real-time bridge)
gkeepapi>=0.17.0
